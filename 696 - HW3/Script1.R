for (i in 1:dim(dataALL)[1]) { 
	# if there is no matching date then adjust the default date back one day
	#
	#test <- macroData$ThreeMonthTBill[min(which(dataALL$Default.Date[i]==macroData$Date))]
	#print(test)
	#print(test)
	
	
}

